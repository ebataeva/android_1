<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.My" parent="Theme.MaterialComponents.Light">
        <item name = "android:textSize">@dimen/textSize</item>
        <!-- Primary brand color. -->
        <item name = "android:background">@color/lemon</item>
        <item name = "android:shadowColor">@color/black</item>
        <item name = "android:solidColor">@color/lemon</item>
        <item name = "android:thumbTextPadding">1dp</item>

        <item name = "android:backgroundTint">@color/lemonTint</item>
        <item name="android:layout_margin">2dp</item>
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="themeWrap" parent="@style/Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:background">@drawable/lemonwebp</item>

    </style>

</resources>